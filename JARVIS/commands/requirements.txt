# Core — required
SpeechRecognition>=3.10.0
PyAudio>=0.2.13
pyttsx3>=2.90

# Commands — install as you enable them
psutil>=5.9.0          # system_info
Pillow>=10.0.0         # screenshot
pycaw>=20181226        # volume control (Windows)
pyautogui>=0.9.54      # media_control
wikipedia-api>=0.6.0   # wikipedia

# Notes:
# PyAudio on Windows often fails via pip. Use instead:
#   pip install pipwin
#   pipwin install pyaudio